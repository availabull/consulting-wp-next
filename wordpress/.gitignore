# ─── Bedrock uploads / caches ───────────────────────────────
web/app/uploads/*
!web/app/uploads/.gitkeep
web/app/cache/*

# ─── WordPress default content we manage elsewhere ──────────
web/wp/
web/.htaccess

# ─── Optional demo theme & build artefacts ──────────────────
web/app/themes/twentytwentyfive/

# ─── Third‑party plugins shipped as zips go here; keep empty dir
web/app/plugins/*.zip

# ─── Logs ───────────────────────────────────────────────────
*.log

# ─── Environment & auth files (never commit secrets) ───────
.env
.env.*
auth.json

# ─── Composer vendor (built in CI / Docker) ─────────────────
/vendor/

# ─── WP‑CLI local overrides (developer only) ───────────────
wp-cli.local.yml

