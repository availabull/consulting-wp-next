# ─────── SMTP / Lets‑Encrypt / Cloudflare ──────────────────────────────
LE_EMAIL=you@example.com
CLOUDFLARE_DNS_API_TOKEN=cf_example_token

# ─────── MariaDB root password (local & CI) ────────────────────────────
MYSQL_ROOT_PASSWORD=local-db-password

# ─────── WordPress GraphQL endpoint the front‑end must call ────────────
# In dev this is proxied by Traefik on port 8080.
NEXT_PUBLIC_WPGRAPHQL_URL=http://localhost:8080/graphql
